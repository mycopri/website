<?php

// $Id: domain_user_default.test,v 1.1.2.1 2008/09/22 20:06:42 jhedstrom Exp $

/**
 * @file
 * Provides SimpleTests for Domain User Default module.
 */

/**
 * Domain User Default SimpleTest class
 */
class DomainUserDefaultTestCase extends DrupalWebTestCase {
  function getInfo() {
    return array(
      'name' => t('Domain User Default tests'),
      'description' => t('Tests various Domain User Default features'),
      'group' => t('Domain Access'),
    );
  }

  /**
   * Verify user creation, default setting and retrieval.
   * @todo: This depends on Domain Access testing suit being able to create
   * domains create domains on the fly.
   */
  function testDomainUserDefault() {
    // create new user with proper permissions

    // $web_user = $this->drupalCreateUserRolePerm(array('set own default domain'));
    // $this->drupalLoginUser($web_user);
  }
}